import React from 'react';
import './App.css';
import {observer} from 'mobx-react-lite';

var App : React.FC = observer(() => {

    return (
      <div>
        <p>HI</p>
      </div>
    )
})

export default App;